"""featherweight-ai — importable project code.

plan.md §4: "Code lives in git. Notebooks are thin launchers that clone the repo
and call into src/ — never the place logic lives."

scripts/ holds one-shot investigations (env checks, dataset inspection, the
Milestone D probe). src/ holds anything a benchmark row depends on.
"""
